server:
  port: 29999

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10000/eureka/

spring:
  application:
    name: jec-service-job
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: jec-config-base